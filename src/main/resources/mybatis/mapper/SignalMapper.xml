<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="yyl.demo.mapper.SignalMapper">

	<resultMap id="RESULT_MAP" type="yyl.demo.entity.Signal">
		<id property="id" column="id" jdbcType="VARCHAR" />
		<result property="value" column="value" jdbcType="BIGINT" />
		<result property="createdBy" column="created_by" jdbcType="VARCHAR" />
		<result property="createdAt" column="created_at" jdbcType="TIMESTAMP" />
		<result property="updatedBy" column="updated_by" jdbcType="BIGINT" />
		<result property="updatedAt" column="updated_at" jdbcType="TIMESTAMP" />
	</resultMap>

	<sql id="TABLE"> signal </sql>

	<sql id="COLUMNS">
		id,value,
		created_by, created_at, updated_by, updated_at
	</sql>

	<sql id="VALUES">
		#{id},#{value},
		#{createdBy}, #{createdAt}, #{updatedBy}, #{updatedAt}
	</sql>

	<sql id="SET_VALUES">
		<trim suffixOverrides=",">
			<if test=" value != null">
				value = #{value},
			</if>
			<if test=" createdBy != null">
				created_by = #{createdBy},
			</if>
			<if test=" createdAt != null">
				created_at = #{createdAt},
			</if>
			<if test=" updatedBy != null">
				updated_by = #{updatedBy},
			</if>
			<if test=" updatedAt != null">
				updated_at = #{updatedAt},
			</if>
		</trim>
	</sql>

	<sql id="WHERES">
		and deleted=0
	</sql>

	<update id="save">
		INSERT INTO
		<include refid="TABLE" />
		(
		<include refid="COLUMNS" />
		) VALUES (
		<include refid="VALUES" />
		)
		ON DUPLICATE KEY UPDATE
		<include refid="SET_VALUES" />
	</update>

	<insert id="insert" parameterType="yyl.demo.entity.Signal">
		INSERT INTO
		<include refid="TABLE" />
		(
		<include refid="COLUMNS" />
		)
		VALUES
		(
		<include refid="VALUES" />
		)
	</insert>

	<delete id="deleteById" parameterType="java.lang.String">
		DELETE FROM
		<include refid="TABLE" />
		WHERE
		id =#{id}
	</delete>

	<delete id="deleteAll">
		DELETE FROM
		<include refid="TABLE" />
	</delete>

	<update id="update" parameterType="yyl.demo.entity.Signal">
		UPDATE
		<include refid="TABLE" />
		<set>
			<include refid="SET_VALUES" />
		</set>
		WHERE
		id = #{id}
	</update>

	<select id="getById" parameterType="java.lang.String" resultMap="RESULT_MAP">
		SELECT
		<include refid="COLUMNS" />
		FROM
		<include refid="TABLE" />
		WHERE id = #{id}
	</select>

	<select id="findAll" resultMap="RESULT_MAP">
		SELECT
		<include refid="COLUMNS" />
		FROM
		<include refid="TABLE" />
	</select>

	<update id="emit">
		UPDATE
		<include refid="TABLE" />
		set value = value + 1, updated_by = NOW(), updated_at = ''
		WHERE id = #{id}
	</update>
</mapper>